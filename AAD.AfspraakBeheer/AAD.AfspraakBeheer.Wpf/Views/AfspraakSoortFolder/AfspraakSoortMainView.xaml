<UserControl x:Class="AAD.AfspraakBeheer.Wpf.Views.AfspraakSoortFolder.AfspraakSoortMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AAD.AfspraakBeheer.Wpf.Views.AfspraakSoortFolder"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ContentControl Content="{Binding AfspraakSoortLijst}" Grid.Column="0" Grid.RowSpan="2"></ContentControl>
        <ContentControl Content="{Binding AfspraakSoortDetail}" Grid.Column="1" Grid.Row="0"></ContentControl>
        <ContentControl Content="{Binding AfspraakSoortToevoegen}" Grid.Row="1" Grid.Column="1"></ContentControl>
    </Grid>
</UserControl>
